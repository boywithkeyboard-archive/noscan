{
  "name": "noscan",
  "version": "1.0.0",
  "description": "A more flexible SDK for unscan",
  "main": "build/index.js",
  "types": "types/index.d.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --minify --outfile=build/index.js && terser build/index.js --compress --mangle --comments false --output build/index.js",
    "dev": "tsc -w",
    "test": "jest",
    "publish:patch": "npm run build && npm version patch && npm publish",
    "publish:minor": "npm run build && npm version minor && npm publish",
    "publish:major": "npm run build && npm version major && npm publish"
  },
  "funding": {
    "type": "individual",
    "url": "https://ko-fi.com/azury"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/azurystudios/noscan.git"
  },
  "keywords": [
    "sdk",
    "asynchronous",
    "scan",
    "minimalistic",
    "malware-detection",
    "easy-to-use"
  ],
  "author": "Azury <hello@azury.dev> (https://azury.dev)",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/azurystudios/noscan/issues"
  },
  "homepage": "https://github.com/azurystudios/noscan#readme",
  "devDependencies": {
    "@types/node": "^16.11.7",
    "chalk": "^5.0.0",
    "esbuild": "^0.14.0",
    "jest": "^27.4.0",
    "node-fetch": "^3.1.0",
    "terser": "^5.10.0",
    "typescript": "^4.4.4"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "type": "module"
}
